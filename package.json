{
  "name": "re_nfc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  
  "scripts": {
    "install": "npm run build && sudo node scripts/service.js && sudo systemctl enable --now nfc.service",
    "uninstall": "sudo systemctl disable --now nfc.service && sudo node scripts/remove.js",
    "build": "tsc -p .",
    "run": "sudo node build/index.js --verbose=true"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "input-event": "^1.2.6",
    "socket.io": "^4.0.0",
    "sqlite3": "^5.0.2"
  },
  "devDependencies": {
    "@types/node": "^14.14.35",
    "@types/express": "^4.17.11",
    "@types/socket.io": "^2.1.13",
    "@types/sqlite3": "^3.1.7",
    "typescript": "^4.2.3",
    "nodemon": "^2.0.7"
  }
}
